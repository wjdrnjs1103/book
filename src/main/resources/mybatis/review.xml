<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.review.ReviewDAOInter">
  <insert id="create" parameterType="dev.mvc.review.ReviewVO">
    INSERT INTO review(reviewno, memberno, productno, title, content, score)
    VALUES(review_seq.nextval, #{memberno}, #{productno}, #{title}, #{content}, #{score })
  </insert>

</mapper>